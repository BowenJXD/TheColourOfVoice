using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


public class ScoreBar_Text : ScoreBar
{
/*    [SerializeField] Text percentText;

    void SetPercentText()
    {
        percentText.text = Mathf.RoundToInt(percentage * 100f);
    }

    public override void Initialize(SplashGrid splashGrid)
    {
        base.Initialize(splashGrid);
        SetPercentText();
    }

    public override void Update(float newPercentage)
    {
        base.Update(float newPercentage);
        SetPercentText();
    }*/
}
